# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# The following `prisma+postgres` URL is similar to the URL produced by running a local Prisma Postgres 
# server with the `prisma dev` CLI command, when not choosing any non-default ports or settings. The API key, unlike the 
# one found in a remote Prisma Postgres URL, does not contain any sensitive information.

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/issue_tracker?schema=public"
PORT=3000
JWT_SECRET="super-secret-key"
SHADOW_DATABASE_URL="postgresql://postgres:postgres@localhost:5432/issue_tracker_shadow?schema=public"


BCRYPT_SALT_ROUNDS=10

JWT_ACCESS_SECRET=super_secret_long_key_here
JWT_ACCESS_EXPIRES=1h
JWT_REFRESH_SECRET=another_secret_key_here
JWT_REFRESH_EXPIRES=7d
JWT_REFRESH_EXPIRES_LONG=30d
JWT_REFRESH_EXPIRES_SHORT=4h


EMAIL_USER=codechefs.rahnemacollege@gmail.com
EMAIL_PASS=FantasticCodeChefs
APP_URL=http://localhost:3000 
